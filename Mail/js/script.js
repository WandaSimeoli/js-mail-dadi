const click = document.getElementById('click');

click.addEventListener('click', 
function() {

const arr = [ email, @, effe, 
];
const value = effe;
 
const isFound = arr.includes(value);
console.log(isFound);
}
);